import 'package:flutter/material.dart';
/*
//En modo claro
const tipoBrilloApp = Brightness.light;

const colorApp = Color.fromRGBO(255, 255, 255, 1);

const colorBottomNavigationBar = Color.fromRGBO(255, 255, 255, 0.75);
const colorLetras = Color.fromRGBO(82, 82, 82, 1);
const colorAcento = Color.fromRGBO(19, 15, 38, 1);
const colorAcentoAlternativo = Color.fromRGBO(240, 240, 246, 1);
const colorAcentoBlur = Colors.white54;

const margenDefault = 20.0;

const tamanioSubtitulos = 18.0;
*/

//En modo oscuro
const tipoBrilloApp = Brightness.dark;

const colorApp = Color.fromRGBO(0, 0, 0, 1);

const colorBottomNavigationBar = Color.fromRGBO(0, 0, 0, 0.75);
const colorLetras = Color.fromRGBO(220, 220, 220, 1);
const colorAcento = Color.fromRGBO(19, 15, 38, 1);
const colorAcentoAlternativo = Color.fromRGBO(40, 40, 46, 1);
const colorAcentoBlur = Colors.black54;

const margenDefault = 20.0;

const tamanioSubtitulos = 18.0;
